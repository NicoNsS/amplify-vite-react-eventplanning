import { Heading, View } from "@aws-amplify/ui-react"; 

export default function InvitationsPage() { 
  return (
    <View padding="1rem">
      <Heading level={1}>Einladungen</Heading>
    </View>
  );
}

import { Heading, View } from "@aws-amplify/ui-react"; 

export default function CalendarPage() { 
  return (
    <View padding="1rem">
      <Heading level={1}>Kalender</Heading>
    </View>
  );
}
